# Third-Party Notices

This project includes third-party open-source software.

- Project: `plought`
- Project license: `AGPL-3.0-or-later` (see `LICENSE.md`)
- Package manager: `npm`

## Dependency Sources

- Direct dependencies are declared in `package.json`.
- Resolved direct + transitive dependencies are recorded in `package-lock.json`.
- Full license texts/notices are available in each installed package under `node_modules/<package>/` (for example `LICENSE`, `LICENSE.md`, or `package.json` license metadata).
